import 'package:flutter/material.dart';

class SocialPlace extends StatelessWidget {
  const SocialPlace({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
